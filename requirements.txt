# Fake News & Misinformation Detector — pinned by major version (Python 3.11).
# Install with `make setup` (uses uv if available, else pip).
# torch: the default PyPI Linux wheel bundles CUDA 12.x and runs on the RTX 2050/3050-class 4 GB GPU;
#        it also works CPU-only. For a smaller CPU-only image use --index-url https://download.pytorch.org/whl/cpu

# --- core data / ML ---
numpy>=1.26,<3
pandas>=2.2,<3
pyarrow>=16,<22
scipy>=1.13,<2
scikit-learn>=1.5,<2
joblib>=1.4,<2
datasketch>=1.6,<2
langdetect>=1.0.9,<2
tqdm>=4.66,<5
requests>=2.32,<3
python-dotenv>=1.0,<2
pyyaml>=6,<7

# --- NLP ---
nltk>=3.9,<4
spacy>=3.7,<4
torch>=2.3,<3
transformers>=4.44,<5
datasets>=3,<5
sentence-transformers>=3.0,<6
faiss-cpu>=1.8,<2
accelerate>=0.30,<2

# --- plotting / EDA ---
matplotlib>=3.9,<4
seaborn>=0.13,<1
wordcloud>=1.9,<2

# --- notebooks ---
jupyter>=1.0,<2
notebook>=7,<8
nbconvert>=7,<8
ipykernel>=6,<7

# --- tests / lint ---
pytest>=8,<9
ruff>=0.5,<1
